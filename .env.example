# Things3 MCP Server Environment Variables

# Things3 Authentication Token (required for update operations)
# You can find this in Things3 > Settings > Help > Authorization Token
THINGS3_AUTH_TOKEN=your_auth_token_here

# Optional: Log level (debug, info, warn, error)
LOG_LEVEL=info

# Optional: Cache TTL in milliseconds
CACHE_TTL=300000